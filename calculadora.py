# Funciones
def sumar(x, y): return x + y
def restar(x, y): return x - y
def multiplicar(x, y): return x * y
def dividir(x, y):
    if y == 0:
        return "No puedes dividir entre cero, vuelve a intentarlo."
    return x / y
